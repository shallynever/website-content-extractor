interface:
  display_name: "Website Content Extractor"
  short_description: "按网站类型匹配 Chrome profile 并提取页面内容"
  default_prompt: "使用 $website-content-extractor，帮我识别这个 URL 的网站类型，匹配专用 Chrome profile，并提取页面正文；如果遇到验证页，就打开对应 profile 让我验证一次。"

policy:
  allow_implicit_invocation: true
